# ENG-004: Git Workflow Standard

**Status:** Draft

Use short-lived branches such as `docs/refine-srs`, `feature/crew-manager`, and `fix/audio-pop`.

Use scoped imperative commits such as `docs: refine product priorities` and `fw: add watchdog recovery`.
