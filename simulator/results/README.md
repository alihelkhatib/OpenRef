# Simulator Results

This directory is the default destination for generated simulator traces and
summaries.

CSV and JSON outputs are ignored by Git. Regenerate them from the scenario files
when needed so evidence stays tied to the simulator version and scenario inputs.
